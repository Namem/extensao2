# Ceres Diagnóstico — dependências mínimas do backend
# Use este arquivo no notebook (sem GPU, sem TensorFlow completo)
#
# Instalação:
#   pip install -r requirements_minimal.txt
#
# Para inferência TFLite sem instalar TensorFlow completo:
#   pip install tflite-runtime
#
# Para background augmentation (se necessário no notebook):
#   pip install rembg onnxruntime

# --- Django ---
Django==6.0.4
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
psycopg2-binary==2.9.11
PyJWT==2.12.1
sqlparse==0.5.5
python-dotenv==1.0.1

# --- MQTT ---
paho-mqtt==2.1.0

# --- IA / Inferência ---
numpy>=1.24
Pillow>=10.0
tqdm>=4.64

# --- Visualização (gráficos TCC) ---
matplotlib>=3.7

# --- Utilitários ---
openpyxl>=3.1
